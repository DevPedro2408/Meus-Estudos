const nomes = ["Pedro", "João", "Breno", "Thiago", "Lima"]

const nomes2 = ["Pedro", "Andre", "Wendeson", "Marcelo", "jeovana"]

function percorrer(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i])
    }
}

percorrer(nomes)

percorrer(nomes2)